Introduction
============

This package contains a simple patch to force all emails send by the IMailDelivery to be sent to a single address, eionet.testing+<old email quoted>@gmail.com.

To install it, include it in the list of eggs and include its zcml.

